{
  "name": "motrac-v2",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --prefix apps/web",
    "android": "npm run android --prefix apps/mobile",
    "ios": "npm run ios --prefix apps/mobile"
  }
}
